const state = {
    formModel: {
    },
    modelSize: 'default', // large 、default 和 small   输入框高度为 40px 32px 和 24px ，默认高度为 32px
    tableSize: 'small' // large 、default 和 small   输入框高度为 40px 32px 和 24px ，默认高度为 32px
};

const getters = {
};

const mutations = {
};

const actions = {};

export default {
    state,
    getters,
    actions,
    mutations
};
